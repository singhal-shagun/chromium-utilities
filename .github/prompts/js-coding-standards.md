# Javascript Coding Standards

1. Write ES modules (ESM) with `import`/`export` syntax. Avoid CommonJS (`require`, `module.exports`) unless necessary for Node.js compatibility.
2. Format all code with prettier tabWidth=4.
